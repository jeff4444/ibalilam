# ============================================
# REQUIRED - Core Application Configuration
# ============================================
# These variables are required for the app to function

# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Application URL
# Used for generating absolute URLs (emails, notifications, payment callbacks)
# Priority: NEXT_PUBLIC_APP_URL > NEXT_PUBLIC_BASE_URL > NEXT_PUBLIC_SITE_URL > VERCEL_URL (auto)
# For local development, defaults to http://localhost:3000 if not set
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Alternative names (for backward compatibility):
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
# NEXT_PUBLIC_SITE_URL=http://localhost:3000


# ============================================
# REQUIRED FOR ADMIN OPERATIONS
# ============================================
# Service role key is required for server-side admin operations
# Get this from your Supabase project settings (keep it secret!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here


# ============================================
# REQUIRED FOR PAYMENT PROCESSING (PayFast)
# ============================================
# Get these from your PayFast merchant dashboard: https://www.payfast.co.za/

# PayFast Merchant Credentials
MERCHANT_ID=your-merchant-id-here
MERCHANT_KEY=your-merchant-key-here

# PayFast Passphrase (Optional but recommended for security)
# Set this in PayFast dashboard under Settings → Integration
PAYFAST_PASSPHRASE=your-passphrase-here

# PayFast API URL
# Use sandbox URL for testing, production URL for live payments
PAYFAST_URL=https://sandbox.payfast.co.za/eng/process
# PAYFAST_URL=https://www.payfast.co.za/eng/process  # Production


# ============================================
# SECURITY & AUTHENTICATION
# ============================================

# CSRF Protection Secret
# Used for CSRF token generation. Falls back to SUPABASE_SERVICE_ROLE_KEY if not set
# Generate a random secret: openssl rand -base64 32
CSRF_SECRET=your-csrf-secret-here

# Cron Job Secret
# Used to secure cron endpoints (e.g., /api/cron/cleanup-reservations)
# Generate a random secret: openssl rand -base64 32
# If not set, cron endpoint will be accessible (development only)
CRON_SECRET=your-cron-secret-here


# ============================================
# OPTIONAL - Development/Testing
# ============================================
# These are for local development and testing only

# Skip PayFast IP validation (development only)
# Set to "true" to bypass IP checks when testing PayFast webhooks locally
# WARNING: Never enable this in production!
PAYFAST_SKIP_IP_CHECK=false

# Skip PayFast server-to-server validation (development only)
# Set to "true" to bypass server validation when testing PayFast IPN locally
# WARNING: Never enable this in production!
PAYFAST_SKIP_SERVER_VALIDATION=false


# ============================================
# OPTIONAL - Email Service (Resend)
# ============================================
# Currently commented out in code. Uncomment to enable email sending.
# Sign up at https://resend.com to get an API key
# RESEND_API_KEY=your-resend-api-key-here


# ============================================
# OPTIONAL - Push Notifications (OneSignal)
# ============================================
# Currently commented out in code. Uncomment to enable push notifications.
# Sign up at https://onesignal.com to get credentials
# ONESIGNAL_API_KEY=your-onesignal-api-key-here
# ONESIGNAL_APP_ID=your-onesignal-app-id-here


# ============================================
# AUTOMATIC - Set by Next.js/Vercel
# ============================================
# These are automatically set by the platform - you don't need to configure them

# NODE_ENV=production|development  # Set automatically by Next.js
# VERCEL_URL=your-app.vercel.app   # Set automatically by Vercel (if deployed)
